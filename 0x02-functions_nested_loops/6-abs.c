#include "main.h"

/**
 * _abs - to write a program that computes the abs value of an int
 * @n: the integer in the question
 *
 * Return: the abs value of the int.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
